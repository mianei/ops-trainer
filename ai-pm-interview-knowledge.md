---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 4320403802296071_0-data_volume/7652292055154114857-files/所有对话/主对话/AI产品经理面试知识库.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 4320403802296071#1783068109610
    ReservedCode2: ""
---
# AI 产品经理面试知识库

> 本文档用于 cvassistant 知识库（RAG）扩充。当用户练习 AI 产品经理面试时，cvassistant 可检索本文档中的相关知识点，作为评分参考和追问素材。

---

# 第一篇：LLM 基础与核心技术

## 1.1 LLM 是什么

大语言模型（Large Language Model）是通过海量文本数据训练出来的神经网络，核心能力是**预测下一个 token**。给定一段文本，模型预测最可能的下一个词，不断重复这个过程就生成了完整回答。

**关键认知**：LLM 不是"理解"语言，而是"统计上最合理地续写"。它没有真正的理解能力，但续写能力的涌现让它看起来像理解了。

### Token

Token 是 LLM 处理文本的最小单位。一个中文字通常等于 1-2 个 token，一个英文单词通常等于 1-3 个 token。"我是产品经理"约 5-6 个 token，"Hello World"约 2 个 token。

**产品意义**：Token 是计费单位，也是上下文窗口的计量单位。控制 prompt 长度就是控制 token 消耗。

### 上下文窗口

上下文窗口是模型一次能"看到"的最大 token 数。超过窗口的内容会被截断或遗忘。

| 模型 | 上下文窗口 |
|------|----------|
| GPT-3.5 | 4K / 16K |
| GPT-4 | 8K / 32K / 128K |
| Claude 3 | 200K |
| Gemini 1.5 Pro | 1M |

**产品意义**：上下文窗口不是越大越好——窗口越大，推理越慢、越贵。产品设计上要控制每次对话的上下文长度，而不是无脑塞满。

## 1.2 LLM 的训练流程

### 预训练（Pre-training）

在海量互联网文本上让模型学会"预测下一个词"。GPT-3 用了约 45TB 文本。产出的是"基础模型"——它知道很多事实，但不知道怎么和人对话。

### 监督微调（SFT）

用高质量"问题-回答"对教模型怎么对话。模型学会了对话格式、礼貌用语、拒绝不合适请求。如果你用 API，这部分是模型厂商做的。

### RLHF（人类反馈强化学习）

让人类对模型的多条回答排序，用这个排序信号训练奖励模型，再用奖励模型优化 LLM。RLHF 让模型更"对齐"人类偏好，但也可能让模型变得过于保守。

### 训练全景

```
互联网文本 → 预训练 → 基础模型（会续写，不会对话）
高质量对话数据 → SFT → 对话模型（会对话，但不够好）
人类偏好排序 → RLHF → 对齐模型（更有用、更安全）
```

## 1.3 RAG vs 微调

| | RAG | 微调（Fine-tuning） |
|------|-----|------|
| 改什么 | 改输入（检索外部知识注入） | 改模型（更新模型参数） |
| 知识更新 | 更新知识库即可 | 需要重新训练 |
| 成本 | 检索 + 推理 | 训练 GPU 成本 |
| 可解释性 | 高，知道从哪检索的 | 低，不知道模型怎么学的 |
| 幻觉控制 | 有事实锚点 | 依赖训练数据质量 |

**用 RAG**：知识频繁变化、需要引用来源、有明确知识库。

**用微调**：需要模型学会特定风格或推理模式、领域术语非常特殊。

大部分场景用 RAG 就够了。微调是试了所有 prompt 和 RAG 优化后还是不行的最后手段。

## 1.4 模型推理与部署

### 推理关键指标

| 指标 | 说明 | 产品意义 |
|------|------|---------|
| 首 token 延迟 | 从请求到第一个 token 出现的时间 | 用户感知的"响应速度" |
| 生成速度 | 每秒生成多少 token | 长回答的等待时间 |
| 吞吐量 | 每秒处理多少请求 | 并发用户数上限 |
| 成本 | 每 1K token 的价格 | 产品定价和利润 |

### 推理优化技术

| 技术 | 是什么 | 产品意义 |
|------|--------|---------|
| 量化 | 把模型参数精度降低（FP16→INT4） | 更快更便宜，精度可能下降 |
| 缓存 | 相同请求复用结果 | 高频问题零成本响应 |
| 流式输出 | 逐 token 输出 | 用户感知更快 |
| 批处理 | 多个请求合并处理 | 离线场景降成本 |

### 模型选择的产品判断

选模型不是选最好的，是选最合适的：任务难度、延迟要求、成本预算、数据安全、语言能力。

## 1.5 Prompt 的底层原理

LLM 的本质是"给定上文，预测下文"。Prompt 就是人为构造的"上文"，引导模型往你期望的方向续写。Prompt 不是"命令"，是"引子"。

### In-Context Learning（上下文学习）

LLM 能从 Prompt 中的例子"学会"怎么做，而不需要更新参数。Few-shot Prompt 就是利用这个能力——给几个例子，模型就能学会你想要的格式和逻辑。

### 为什么 Prompt 有时候不灵

- **位置偏差**：模型对 Prompt 开头和结尾更关注，中间易被忽略
- **长度偏差**：Prompt 越长，模型越容易"走神"
- **指令冲突**：多条矛盾指令，模型不知道听谁的
- **格式干扰**：Prompt 格式混乱，输出也跟着乱

## 1.6 多模态模型

| 能力 | 代表模型 | 应用场景 |
|------|---------|---------|
| 文生文 | GPT-4, Claude | 对话、写作、分析 |
| 文生图 | DALL-E, Midjourney | 设计、营销素材 |
| 图生文 | GPT-4V, Claude 3 | 图片分析、OCR |
| 文生音 | ElevenLabs | 有声内容、语音助手 |
| 音生文 | Whisper | 语音转文字 |
| 文生视频 | Sora, Runway | 视频创作 |

**产品判断**：用户输入是图片/音频→必须用多模态；纯粹文字场景→不用，成本更高。

---

# 第一篇·续：核心模型架构详解

> 这一篇是 LLM 基础的深化。面试官问"你用过 BERT 吗""Transformer 为什么重要""GPT 和 BERT 有什么区别"，答案都在这里。

## 1.7 Transformer：一切大模型的基石

### 为什么 Transformer 是革命性的

2017 年 Google 提出 Transformer 之前，NLP 主流是 RNN（循环神经网络）和 LSTM（长短期记忆网络）。它们的问题很致命：

| 问题 | RNN/LSTM | Transformer |
|------|---------|-------------|
| 处理方式 | 逐词串行处理，"我"→"是"→"产品"→"经理" | 所有词同时处理（并行） |
| 长文本 | 读到后面忘了前面（梯度消失） | 自注意力机制，每个词都能直接看到所有其他词 |
| 训练速度 | 慢，无法并行 | 快，GPU 并行计算 |
| 上下文理解 | 距离远的词很难关联 | 任意两个词之间距离都是 1 |

**一句话**：Transformer 用"自注意力（Self-Attention）"替代了"循环"，让模型可以同时处理整段文本，这是 GPT、BERT、Claude 一切现代大模型的地基。

### Transformer 的核心结构

```
输入文本 → ①词嵌入（把词变向量）→ ②位置编码（告诉模型词的位置）→ ③编码器（N层，理解语义）→ ④解码器（N层，逐词生成）→ 输出文本
```

**编码器（Encoder）**：理解输入文本。读完一句话，输出一个"理解过的"向量表示。BERT 只用编码器。

**解码器（Decoder）**：生成输出文本。基于编码器的理解（或纯自回归），逐词生成。GPT 只用解码器。

### 自注意力机制（Self-Attention）：核心中的核心

**通俗理解**：读"我买了一本书，它非常好看"，"它"指"书"——人做这个关联很容易，但传统模型很难。自注意力让每个词都和句中所有其他词"对望"一次，算出彼此的相关度。

**产品视角**：为什么 ChatGPT 能理解你的长问题？因为自注意力让"问题末尾的指令"和"问题开头的信息"直接关联，中间隔了多少词都不影响。这是 RNN 做不到的。

### 多头注意力（Multi-Head Attention）

**通俗理解**：不是一种注意力，是多种注意力同时看。一个头关注"语法关系"，一个头关注"语义关联"，一个头关注"位置关系"。多个头的结果拼接起来，理解更全面。

**产品意义**：不是你需要调参的这个概念。但面试时能说出"Transformer 的核心是自注意力，多头注意力让模型从不同角度理解文本"——比只说"Transformer 很重要"强得多。

### 位置编码（Positional Encoding）

词嵌入加上位置信息，让模型知道"第一个词"和"最后一个词"的区别。没有位置编码，"我爱你"和"你爱我"在模型眼里就是同一堆词。

### 残差连接与层归一化

这两个是训练技巧，不是架构核心。作用是让深层网络能顺利训练、不梯度消失。产品经理不需要知道细节，但需要知道"Transformer 能堆到上千亿参数，靠的就是这些工程优化"。

## 1.8 GPT 系列：解码器架构

### GPT 的架构选择

GPT 只用 Transformer 的**解码器**部分。这意味着：

- **自回归生成**：一次生成一个 token，用已生成的 token 预测下一个
- **单向注意力**：每个 token 只能看到它前面的 token，看不到后面的
- **天然适合生成**：写文章、对话、代码——都是"往前写"

### GPT-1 → GPT-4 的演进

| 版本 | 时间 | 参数规模 | 关键突破 |
|------|------|---------|---------|
| GPT-1 | 2018 | 1.17 亿 | 证明"预训练+微调"范式可行 |
| GPT-2 | 2019 | 15 亿 | 发现零样本能力，模型够大自己就会做很多事 |
| GPT-3 | 2020 | 1750 亿 | 上下文学习（In-Context Learning），Prompt 里给几个例子模型就会做 |
| GPT-3.5 | 2022 | 未公开 | ChatGPT 的基础，加了 RLHF 对齐 |
| GPT-4 | 2023 | 未公开 | 多模态（文本+图像输入）、推理能力大幅提升 |

### 三个关键概念

**1. 自回归（Autoregressive）**

当前输出取决于之前所有输出。生成"我今天"→预测"去"→生成"我今天去"→预测"了"→……每一步都用上一步的结果。

**2. 预训练 + 对齐**

GPT-3 是"预训练模型"——它会续写任何文本，但不知道什么时候该闭嘴。ChatGPT（GPT-3.5+RLHF）是"对齐后的模型"——它学会了对话、拒绝、承认不知道。

**3. Scaling Law（规模定律）**

模型越大、数据越多、算力越多→性能越好。这个规律在 GPT-4 之前一直成立，是 OpenAI 大力出奇迹的理论基础。

### GPT 的产品意义

面试时不要说"我用了 GPT-4 API"。要说：

- 为什么选 GPT-4 而不是 Claude 或文心？→ 推理能力更强、对复杂 Prompt 的理解更好
- GPT 是自回归模型，生成时逐 token 输出→ 你用了流式输出减少用户等待感
- GPT 的上下文窗口是 128K→ 你控制了每次对话的上下文长度，确保不超窗口

## 1.9 BERT：编码器架构

### BERT 和 GPT 的根本区别

| | GPT | BERT |
|------|------|------|
| 架构 | 只用解码器 | 只用编码器 |
| 注意力方向 | 单向（只看左边） | 双向（左右都看） |
| 适合做什么 | 生成（写文章、对话） | 理解（分类、判断、提取） |
| 训练方式 | 预测下一个词 | 挖空猜词（MLM）+ 判断两句是否连续（NSP） |

**一句话**：GPT 是"写"，BERT 是"读"。GPT 擅长生成，BERT 擅长理解。

### BERT 的训练方式

**MLM（Masked Language Model，掩码语言模型）**

随机把句子中 15% 的词挖掉，让模型猜。比如"我[MASK]产品经理"→模型猜"是"。因为要猜，必须看整句话的上下文——这就是"双向"的来源。

**NSP（Next Sentence Prediction，下一句预测）**

给两句，判断第二句是不是第一句的下一句。这个训练让 BERT 学会理解句子之间的关系。

### BERT 的典型应用

| 任务 | 说明 | 产品场景 |
|------|------|---------|
| 文本分类 | 判断一段文本属于哪个类别 | 用户反馈自动分类（bug/建议/咨询） |
| 命名实体识别（NER） | 提取人名、地名、公司名等 | 从简历中提取学校、公司、技能 |
| 情感分析 | 判断正面/负面/中性 | 评论分析、舆情监控 |
| 语义相似度 | 判断两段文本是否意思相近 | 去重、搜索、FAQ 匹配 |
| 问答系统 | 从给定文本中找答案 | 帮助文档的智能搜索 |

### BERT 的变体

| 变体 | 改进 | 适用场景 |
|------|------|---------|
| RoBERTa | 更大数据、更久训练、去掉 NSP | BERT 的增强版，理解能力更强 |
| ALBERT | 参数共享，更小更快 | 资源受限的部署场景 |
| DistilBERT | 知识蒸馏，轻量化 | 移动端、边缘设备 |

### BERT 的产品意义

面试时如果被问到 BERT：

- 做过分类任务→ 可以说用 BERT 做意图识别（判断用户是"提问"还是"评分"还是"优化"）
- 做过 NER→ 可以说用 BERT 从简历里提取关键信息（学校、公司、技能）
- 没做过→ 可以说"BERT 是编码器架构，适合理解类任务。我的 cvassistant 的意图识别现阶段用正则+LLM 兜底，因为意图类型少。但如果意图扩展到 20+ 种，会考虑用 BERT 微调做分类"

**关键**：能说出"BERT 适合什么场景、不适合什么场景"——这比"我用了 BERT"更有说服力。

## 1.10 编码器-解码器架构（T5、BART 等）

### 什么时候用全架构

Transformer 原论文是编码器+解码器都用的。GPT 只取解码器，BERT 只取编码器。但有些任务需要"理解+生成"全链路——先理解输入，再基于理解生成输出。

### 代表模型

| 模型 | 架构 | 特点 |
|------|------|------|
| T5（Google） | 编码器-解码器 | 把所有 NLP 任务统一成"文本到文本" |
| BART（Meta） | 编码器-解码器 | 把被破坏的文本还原 |
| mT5 | 编码器-解码器 | 多语言版本 |

### 适用场景

翻译、摘要、改写——这些任务需要先"理解原文"（编码器），再"生成目标文本"（解码器）。

### 产品判断

面试中很少被问到 T5/BART，除非你做的是翻译或摘要类产品。知道有这类架构、知道和 GPT/BERT 的区别就够了。

## 1.11 开源大模型阵营

### LLaMA 系列（Meta）

| 版本 | 参数 | 关键意义 |
|------|------|---------|
| LLaMA 1 | 7B-65B | 证明开源模型能接近 GPT-3 |
| LLaMA 2 | 7B-70B | 商用许可，生态爆发 |
| LLaMA 3 | 8B-405B | 性能接近 GPT-4 |

**产品意义**：如果你要私有化部署（数据不出公司），LLaMA 是首选。成本比 GPT-4 API 低，但需要自己搭服务器。

### 其他重要开源模型

| 模型 | 团队 | 亮点 |
|------|------|------|
| Mistral | 法国 Mistral AI | 小参数大能力，7B 模型性能惊人 |
| Qwen（通义千问）| 阿里 | 中文能力突出，多尺寸覆盖 |
| DeepSeek | 深度求索 | MoE 架构，性价比极高 |
| ChatGLM | 智谱 | 中文优化，清华系 |
| Yi | 零一万物 | 李开复团队，长上下文 |

### 闭源 vs 开源的产品决策

```
你需要数据不出公司吗？
  ├─ 是 → 开源模型私有化部署
  └─ 否 → 你需要最好的推理能力吗？
           ├─ 是 → GPT-4 / Claude
           └─ 否 → 开源模型 + API（成本更低）
```

## 1.12 MoE（混合专家）架构

### 什么是 MoE

传统模型每次推理激活所有参数。MoE 每次推理只激活部分参数（比如 8 个"专家"中激活 2 个），大大降低计算量。

**类比**：传统模型像一个全科医生，什么病都自己看。MoE 像一个医院，有心脏科、骨科、皮肤科，每个病人只去对应的科室。

### 代表模型

- **Mixtral 8×7B**：8 个专家，每次激活 2 个，总参数 47B，推理成本相当于 12B 模型
- **DeepSeek-V2**：MoE 架构，训练成本远低于同性能的密集模型
- **GPT-4**：据传也是 MoE 架构（8×220B）

### 产品意义

MoE 的意义是"花小钱办大事"。面试时如果被问到"怎么控制 API 成本"，提到 MoE 是一个加分项——"如果选模型，可以优先考虑 MoE 架构的，推理成本低但能力不差"。

## 1.13 模型架构全景对比

| 架构 | 代表模型 | 擅长 | 不擅长 | 产品场景 |
|------|---------|------|--------|---------|
| 解码器 Only | GPT-4, Claude, LLaMA | 生成、对话、推理 | 需要双向理解的分类 | 聊天、写作、Agent |
| 编码器 Only | BERT, RoBERTa | 分类、NER、语义匹配 | 生成文本 | 意图识别、简历解析 |
| 编码器-解码器 | T5, BART | 翻译、摘要、改写 | 长对话 | 翻译产品、文档摘要 |
| MoE | Mixtral, DeepSeek | 高性价比推理 | 微调难度大 | 需要控制成本的大规模推理 |

## 1.14 模型架构的面试回答模板

**Q："你了解 Transformer 吗？"**

> "Transformer 是当前所有大模型的基础架构。核心创新是用自注意力机制替代了 RNN 的串行处理，让模型可以并行处理整个序列，而且任意两个词之间都能直接建立关联。GPT 用了它的解码器部分做生成，BERT 用了编码器做理解。我的 cvassistant 底层调的是 GPT-4 API，它就是一个纯解码器架构的自回归模型。"

**Q："GPT 和 BERT 有什么区别？"**

> "GPT 是解码器架构，单向注意力，适合生成。BERT 是编码器架构，双向注意力，适合理解。GPT 的训练任务是预测下一个词，BERT 是挖空猜词。所以我做面试评分 Agent 用的是 GPT——因为需要生成评分和反馈。如果我要做意图识别分类，可以考虑微调 BERT。"

**Q："你用过开源模型吗？"**

> "我的 cvassistant 项目用的是 GPT-4 API，因为当时追求最好的推理能力。但如果要私有化部署、控制成本，我会优先考虑 LLaMA 3 或 DeepSeek。选型依据是：数据安全要求、推理能力要求、成本预算。"

---

# 第二篇：AI Agent 全景

## 2.1 Agent 基础概念

Agent（智能体）是一个能感知环境、自主决策、执行动作的 AI 系统。核心特征是可以**拆解任务、调用工具、多步推理、记忆上下文**。

**类比**：传统 LLM = 答题机器，你问它答，答完就忘。Agent = 实习生，能理解你要什么、自己拆步骤、需要时查资料、做完了汇报。

### Agent 的核心四要素

| 要素 | 做什么 | 举例 |
|------|--------|------|
| 规划（Planning） | 把大任务拆成小步骤 | "帮我做竞品分析" → 先搜竞品列表 → 逐个查信息 → 对比 → 输出报告 |
| 记忆（Memory） | 记住上下文和历史 | 用户 10 分钟前说过"我是产品实习生"，Agent 后续回答始终基于这个身份 |
| 工具（Tools） | 调用外部能力 | 搜索、读文件、发邮件、操作飞书文档 |
| 行动（Action） | 执行具体操作 | 调搜索 API → 读网页 → 写文件 → 返回结果 |

### Agent 和传统 Chatbot 的区别

| | Chatbot | Agent |
|------|---------|-------|
| 交互模式 | 一问一答 | 多步推理，自主执行 |
| 上下文 | 单轮或有限多轮 | 长期记忆 + 短期记忆 |
| 能力边界 | 纯文本生成 | 调用工具、读写文件、操作外部系统 |
| 任务复杂度 | 简单问答 | 复杂任务拆解和执行 |

## 2.2 Agent 架构模式

### Fixed Workflow（固定工作流）

预定义的、不可变的执行步骤链。每一步做什么是写死的。

**适用**：RAG 问答、报告生成、面试评分。**优点**：稳定、可预测、易调试、成本低。**缺点**：不灵活。

**产品判断**：能用 Fixed Workflow 搞定的，不要上 Agent。架构越简单越可靠。

### Sub-agent（子 Agent）

主 Agent 把复杂子任务派发给专门的小 Agent，子 Agent 独立完成后返回结果。

**适用**：子任务独立性强、可并行。**优点**：并行提效、职责分离。**缺点**：调度复杂、通信有损耗。

**产品判断**：不要为了"听起来高级"用 Sub-agent。如果子任务有明显先后依赖，串行比并行更可控。

### Agent Team（Agent 团队）

多个 Agent 各司其职，像一个团队协作完成任务。

**适用**：任务需要不同角色协同。**优点**：模拟真实协作。**缺点**：调度复杂、通信开销大、调试困难。

**产品判断**：Agent Team 是最容易被面试官挑战的架构。除非场景确实需要多角色协作，否则不要用。

### 架构选型决策树

```
任务是否固定不变？
  ├─ 是 → Fixed Workflow
  └─ 否 → 任务是否可以拆成独立子任务？
           ├─ 是，且子任务可并行 → Sub-agent
           ├─ 是，且子任务需要不同角色 → Agent Team
           └─ 否 → 单 Agent + 工具调用
```

## 2.3 RAG（检索增强生成）

### 核心流程

```
用户提问 → ①检索（从知识库找相关文档）→ ②增强（拼接检索结果到 Prompt）→ ③生成（LLM 基于拼接后的 Prompt 回答）
```

**为什么需要 RAG**：LLM 训练数据有截止日期、会幻觉、没见过企业私有知识。

### 检索策略对比

| 策略 | 原理 | 擅长 | 不擅长 |
|------|------|------|--------|
| 关键词检索（BM25） | 匹配关键词，算词频 | 精确匹配专业术语 | 同义词、语义相近但措辞不同 |
| 语义向量检索 | 文本转向量，算余弦相似度 | 理解"用户增长"≈"DAU 提升" | 精确匹配时噪声大 |
| 混合检索（Hybrid） | BM25 + 语义向量合并 | 兼顾精确和语义 | 实现复杂度稍高 |
| 多路召回 + 重排序 | 多路粗召回 → 精排重排 | 准确率最高 | 延迟和成本更高 |

### RAG 选型的产品判断

- 知识库 < 100 篇 → 关键词检索就够了
- 知识库 100-1000 篇 → 混合检索（BM25 + 语义）
- 知识库 > 1000 篇 → 多路召回 + 重排序
- 知识频繁更新 → 实时索引

### RAG 常见问题与优化

| 问题 | 现象 | 优化方向 |
|------|------|---------|
| 检索不到 | 知识库有答案但搜不出来 | 优化 chunk 策略、调整 embedding 模型 |
| 检索噪声 | 搜出大量不相关内容 | 加阈值过滤、重排序 |
| 上下文过长 | 拼接后超 token 限制 | 压缩检索结果 |
| 信息冲突 | 多个文档矛盾 | 加来源标注、时效性权重 |
| 答案被检索结果绑架 | 检索到垃圾内容模型照搬 | 加验证步骤 |

## 2.4 Prompt Engineering

### Prompt 的四层解耦结构

```
第一层：角色（Role）——你是谁
第二层：知识（Knowledge）——你知道什么
第三层：任务（Task）——你要做什么
第四层：格式（Format）——你怎么输出
```

**解耦的价值**：每层独立修改，改输出格式不碰评分逻辑；出问题能定位到具体层级；团队协作时不同人负责不同层。

### 常见 Prompt 技巧

| 技巧 | 说明 | 示例 |
|------|------|------|
| Few-shot | 给几个例子，让模型照猫画虎 | 给出 3 个"好回答 + 评分"的例子 |
| Chain-of-Thought | 让模型"一步步思考" | "请先分析用户回答的要点，再逐项评分" |
| 正反例 | 告诉模型什么是对什么是错 | "好：具体到场景。差：空泛的套话" |
| 约束边界 | 明确什么不能做 | "不要给职业建议，不要安抚情绪" |
| 结构化输出 | 要求 JSON/Markdown 格式 | "请严格按 JSON 格式输出" |

### Prompt 迭代的产品方法论

```
发现问题 → 定位到具体 Prompt 层 → 改前跑基线 → 改后跑评测 → 对比分数 → 决定保留/回滚
```

**一条规则**：一次只改一个变量。不要同时改角色定义和评分标准。

## 2.5 Agent 评测

### 评测金字塔

```
         ┌──────────────┐
         │  用户行为指标  │  ← 最难评，但最有价值
         │  （留存、复访） │
         ├──────────────┤
         │  端到端体验指标 │  ← 任务完成率、用户满意度
         │ （任务成功率）  │
         ├──────────────┤
         │  链路节点指标   │  ← 检索准确率、工具调用成功率
         │（检索、工具调用）│
         ├──────────────┤
         │  基础能力指标   │  ← 格式正确率、响应时间
         │ （格式、延迟）  │
         └──────────────┘
```

### 评测集设计：MECE 原则

维度之间互不重叠、共同穷尽。以面试评分 Agent 为例：

| 维度 | 评什么 | 不评什么 |
|------|--------|---------|
| 内容相关性 | 回答是否在回答问题 | 不评表达好不好 |
| 逻辑结构 | 是否有清晰的因果/递进 | 不评内容是否深刻 |
| 表达清晰度 | 是否简洁准确无歧义 | 不评逻辑是否严密 |
| 岗位匹配度 | 是否体现岗位能力 | 不评表达是否流畅 |
| 深度与细节 | 是否有具体案例和数据 | 不评是否匹配岗位 |

### 评测执行方式

| 方式 | 说明 | 适用场景 |
|------|------|---------|
| 人工标注 | 人打分，作为黄金标准 | 建立基线、校准模型 |
| LLM-as-a-Judge | 用 LLM 打分 | 规模化评测、快速迭代 |
| 规则检查 | 用正则/代码检查格式 | 输出格式校验 |
| A/B 测试 | 分流对比 | 已上线产品、有真实用户 |

### 评测指标速查

| 指标 | 定义 | 适用场景 |
|------|------|---------|
| 任务完成率 | 用户意图是否被满足 | 端到端评测 |
| 意图理解准确率 | Agent 是否正确理解用户意图 | 意图识别模块 |
| 工具调用准确率 | 工具选择是否正确、参数是否准确 | 工具调用模块 |
| 检索命中率 | 检索结果是否包含正确答案 | RAG 检索节点 |
| 幻觉率 | 输出中虚构信息的比例 | 生成质量评测 |
| 格式正确率 | 输出是否符合预期格式 | 输出解析 |
| 用户满意度 | 用户主观评分 | 上线后追踪 |
| 多轮对话成功率 | 多轮后是否完成目标 | 对话类 Agent |

## 2.6 Agent 记忆与上下文

### 记忆类型

| 类型 | 存什么 | 多久 | 举例 |
|------|--------|------|------|
| 短期记忆 | 当前对话历史 | 一次会话 | 用户刚才说了什么 |
| 长期记忆 | 用户画像、偏好 | 跨会话 | 用户是产品实习生 |
| 工作记忆 | 当前任务的状态 | 一个任务周期 | 正在搜第 3 个竞品 |

### 上下文窗口管理策略

| 策略 | 做法 | 优缺点 |
|------|------|--------|
| 滑动窗口 | 只保留最近 N 轮对话 | 简单，但丢失早期信息 |
| 摘要压缩 | 对早期对话做摘要 | 保留要点，但摘要可能失真 |
| 选择性遗忘 | 根据重要性决定保留什么 | 最优，但判断"重要性"本身很难 |
| 分层记忆 | 短期+长期+工作记忆分层管理 | 最接近人类记忆，架构复杂 |

### 产品判断：记忆设计的核心问题

1. 用户回来时 Agent 还记得他吗？记不住→每次重新介绍→体验差
2. 记住的信息会过期吗？用户换了岗位 Agent 还在用旧信息→体验更差
3. 记忆会不会泄露？用户 A 的对话被用来回答用户 B→这是事故

## 2.7 工具调用

### 工具设计原则

| 原则 | 说明 |
|------|------|
| 单一职责 | 一个工具只做一件事 |
| 明确输入输出 | 参数名、类型、必填/可选、返回值结构都要清楚 |
| 错误处理 | 工具调用失败时 Agent 要知道怎么处理（重试/降级/报告） |
| 幂等性 | 同样的输入多次调用，结果应该一致 |
| 超时控制 | 外部 API 调用要有超时 |

### 工具调用的常见坑

| 坑 | 表现 | 解法 |
|------|------|------|
| 幻觉调用 | Agent 调了不存在的工具 | 严格定义工具列表 |
| 参数错误 | 传了错误的参数类型 | 参数校验+重试 |
| 循环调用 | Agent 反复调同一个工具 | 设置最大调用次数 |
| 过早调用 | 还没理解意图就调工具 | 先做意图识别 |
| 结果误用 | 工具返回了结果但 Agent 用错了 | 工具返回附带使用说明 |

## 2.8 主流 Agent 框架

| 框架 | 定位 | 适合谁 |
|------|------|--------|
| Coze（扣子） | 零代码 Agent 开发平台 | 产品经理、非技术人员 |
| LangChain | Python/JS 开发框架 | 有编程能力的开发者 |
| AutoGPT | 自主任务执行 Agent | 探索性项目、Demo |
| CrewAI | 多 Agent 协作框架 | 需要多角色协作的场景 |
| Dify | 低代码 LLM 应用开发平台 | 产品经理、快速原型 |
| Semantic Kernel | 微软的 AI 编排框架 | 企业级、.NET 生态 |

**产品判断**：框架选型是产品问题。问三个问题：谁在用？要上线还是做 Demo？需要多 Agent 吗？

## 2.9 Agent 安全与护栏

### 常见安全风险

| 风险 | 说明 | 防护 |
|------|------|------|
| Prompt 注入 | 用户输入覆盖了系统指令 | 输入过滤、角色分离、指令优先级 |
| 越狱 | 用户诱导 Agent 绕过限制 | 多层防护、输出审核 |
| 数据泄露 | Agent 输出中包含其他用户数据 | 记忆隔离、输出脱敏 |
| 幻觉 | Agent 编造不存在的事实 | RAG 提供事实锚点、输出验证 |
| 滥用 | 用户用 Agent 做违规操作 | 使用限制、内容审核 |

### 护栏设计层次

```
第一层：输入过滤 → 第二层：指令约束 → 第三层：工具限制 → 第四层：输出审核 → 第五层：人工兜底
```

**产品判断**：安全是产品设计的一部分。产品经理需要定义什么场景转人工、输出审核标准是什么、用户数据在链路中经过哪些节点。

---

# 第三篇：AI 产品经理角色与能力

## 3.1 AI PM vs 传统 PM

| | 传统 PM | AI PM |
|------|---------|-------|
| 核心产出 | 功能需求文档、原型、PRD | 上述 + 评测集、Prompt 策略、Badcase 分类 |
| 技术边界感 | 懂技术原理即可 | 必须懂 AI 能做什么、不能做什么、成本多少 |
| 不确定性 | 需求相对确定，效果可预期 | 模型输出不确定，效果需要持续迭代 |
| 迭代方式 | 上线→看数据→改功能 | 上线→看 Badcase→改 Prompt/链路→重新评测 |
| 成功标准 | 用户增长、留存、转化 | 上述 + 任务完成率、准确率、幻觉率 |
| 协作对象 | 工程师、设计师、运营 | 上述 + 算法工程师、标注团队 |

### AI PM 不需要 vs 需要

**不需要**：手写模型训练代码、自己训模型、会调 CUDA、看懂 Transformer 数学公式。

**需要**：知道这些概念是干什么的能和技术团队对话、能判断什么场景该用什么技术方案、能设计评测方案。

## 3.2 AI 产品经理的核心能力

### 技术边界感

知道当前 AI 技术能做什么、不能做什么。

**能做什么**：文本理解与生成、信息检索与总结、代码辅助、翻译、分类、情感分析、简单推理。

**不能做什么**：精确数学计算、100% 事实准确、真正的推理（不是统计续写）、理解物理世界、保证不产生幻觉。

**面试体现**：面试官问"你怎么用 AI 解决 XX"，你能说出"这个场景 LLM 搞不定，因为 XX"——这比"我可以用 Agent 做"更能证明边界感。

### 评测体系设计

不是"感觉好不好用"，而是能设计可量化、可复用的评测标准。

**核心能力**：定义 MECE 评测维度、设计评测集（样本来源、数量、标注方式）、选择评测执行方式、从 Badcase 反推产品改进。

**面试体现**：不要说"我们提高了准确率"，要说"用 30 道 Golden Set，五维评分，Spearman 校准后一致性从 0.70 到 0.85"。

### Prompt Engineering 产品化

不是"写一段 prompt"，而是管理 prompt 的完整生命周期。

**核心能力**：设计 Prompt 结构（分层解耦）、迭代 Prompt（基线→修改→评测→对比→决定）、A/B 测试 Prompt、管理 Prompt 版本。

**面试体现**：不要说"我优化了 prompt"，要说"我定位到第三层任务指令的评分规则缺少岗位锚定，增加了 5 条规则后，岗位匹配度从 2.1 到 4.3"。

### Badcase 驱动迭代

不是"上线了就算完"，而是建立发现→诊断→修复→验证的闭环。

**核心能力**：从用户反馈/评测数据中发现 Badcase、诊断根因（是 Prompt 问题、检索问题、还是链路问题）、给出修复方案、用同一套评测集验证。

**面试体现**：讲一个具体的 Badcase，说清楚现象、诊断过程、修复动作、验证结果。不要只说"改了一下 prompt 就好了"。

### 数据敏感度

不是"会写 SQL"，而是能定义数据口径、判断数据质量。

**核心能力**：每个指标都知道样本量、定义、口径、基线；能判断数据异常是真实问题还是统计偏差；能从数据中发现问题，而不是被数据带着走。

**面试体现**：提到的每个数字都能说出"样本量 30 题、人工标注为基准、Spearman 校准"——这就是数据敏感度。

## 3.3 AI 产品经理的常见岗位类型

| 岗位 | 核心工作 | 适合谁 |
|------|---------|--------|
| AI 应用 PM | 用现有 AI 能力做产品 | 大多数 AI PM |
| AI 平台 PM | 做 AI 开发平台/工具 | 有技术背景的 PM |
| AI 策略 PM | 定义 AI 产品方向和安全边界 | 有经验的高级 PM |
| 大模型 PM | 参与模型训练和优化 | 有算法背景的 PM |

**实习生/校招**：90% 是 AI 应用 PM。面试官不指望你训过模型，但希望你能说清楚你做的产品里 AI 怎么用的、怎么评测的、怎么迭代的。

## 3.4 AI 产品经理的成长路径

```
实习生：做一个完整的 AI 项目，建立评测意识
  ↓
初级 PM：独立负责一个 AI 功能模块，有真实用户数据
  ↓
中级 PM：独立负责一个 AI 产品，有完整的评测和迭代体系
  ↓
高级 PM：定义 AI 产品方向，管理多个 AI 产品，搭建团队
```

### 实习生阶段该做和不该做的

**该做的**：1-2 个完整的 AI 项目（可以是个人项目）、有一套评测体系（哪怕只是 30 题 Golden Set）、有几个能讲清楚的 Badcase 迭代案例、能说清楚每个技术决策的"为什么"。

**不该做的**：简历上写"负责从 0 到 1 搭建 AI 平台"（实习生做不到）、罗列技术名词但不解释为什么用、虚构没有的数据。

---

# 第四篇：AI 产品设计方法论

## 4.1 核心原则

### 不确定性是默认状态

传统产品输入输出确定——点按钮就跳转。AI 产品不同——同一个 prompt 两次结果可能不同。这是本质，不是 bug。

**产品设计含义**：不要设计"AI 必须 100% 准确"的产品（做不到）；设计"AI 可以犯错但用户能纠正"的产品；给 AI 输出留"退路"——用户可以编辑、重试、跳过。

### 用户预期管理比模型能力更重要

用户对 AI 的预期往往高于实际能力。产品经理的首要任务是管理预期，而非提升模型。

**产品设计含义**：明确告诉用户 AI 能做什么不能做什么；AI 输出不确定时标注"仅供参考"或给出置信度；高风险场景明确提醒"请咨询专业人士"。

### 渐进式信任

用户对 AI 的信任是逐步建立的。

**产品设计含义**：第一阶段 AI 做辅助（建议、参考），用户自己决策；第二阶段 AI 做半自动（AI 执行，用户确认）；第三阶段 AI 做全自动（AI 执行，用户事后检查）。不要一上来就做全自动。

## 4.2 交互模式

| 模式 | 形态 | 代表产品 | 适合场景 | 不适合场景 |
|------|------|---------|---------|----------|
| 对话式 | 自然语言对话 | ChatGPT、Claude | 开放式任务、探索性场景 | 高频重复操作、精确输入 |
| 辅助式（Copilot） | 嵌入工作流 | GitHub Copilot、Notion AI | 用户已有明确工作流 | AI 主导决策 |
| 自主式（Agent） | AI 自主完成 | AutoGPT、Devin | 任务明确、出错成本低 | 需要人类判断、出错成本高 |
| 嵌入式 | AI 在幕后 | 搜索 AI 摘要、电商推荐 | AI 是增强，用户无感 | AI 是核心卖点 |

### 对话式设计要点

首轮对话给用户"引导"——告诉用户能问什么；多轮对话要有"记忆"——用户说过的不要重复问；错误处理要友好——AI 没理解时给用户一个"换个问法"的提示。

### 辅助式设计要点

AI 不要打断用户；AI 提供"建议"而非"替代"——用户可以选择接受或忽略；出错成本要低——一键撤销 AI 的修改。

### 自主式设计要点

必须有"中途干预"——用户可以在任何时候暂停、修改、取消；必须让用户"看到过程"——AI 在做什么，做到哪一步了；必须有"结果确认"——用户确认再执行不可逆操作。

## 4.3 错误处理设计

### 错误类型与应对

| 错误类型 | 表现 | 应对 |
|---------|------|------|
| 幻觉 | AI 编造了不存在的信息 | 标注来源、提供"核实"按钮 |
| 理解偏差 | AI 误解了用户意图 | 提供"换个说法"的引导 |
| 能力边界 | 用户要求 AI 做不到的事 | 明确告知能力边界 |
| 格式错误 | AI 输出格式不符合预期 | 重试按钮、格式自动修复 |
| 超时 | AI 响应太慢或卡住 | 超时提示、降级方案 |
| 工具调用失败 | AI 调用的外部 API 挂了 | 降级处理、告知用户、允许重试 |

### 错误处理四原则

1. **永远不要让用户看到"系统错误"**——把技术错误翻译成用户能理解的语言
2. **给用户一个"下一步"**——不要只说"出错了"，要告诉用户能做什么
3. **错误是学习机会**——每次错误都应该"学到"这个 case，下次不再犯
4. **降低错误的代价**——AI 输出如果能一键撤销、一键修改，用户对错误的容忍度会大幅提升

## 4.4 数据飞轮

```
用户使用 → 产生数据 → 优化模型 → 体验变好 → 更多用户使用 → 更多数据
```

| 环节 | 产品设计要点 |
|------|------------|
| 收集数据 | 用户点赞/踩、修改 AI 输出、复制/不复制、继续追问/放弃 |
| 分析数据 | 哪些 case 用户踩了？哪些场景用户放弃了？ |
| 优化模型 | 用 Badcase 数据迭代 Prompt/RAG/微调 |
| 验证效果 | 用同一套评测集验证，确认体验变好了 |

**常见陷阱**：没有收集数据、收集了不分析、分析了不行动、行动了不验证。

## 4.5 产品形态决策树

### 选什么产品形态

```
用户有明确的任务吗？
  ├─ 没有 → 对话式（让用户探索）
  └─ 有 → 任务可以自动化吗？
           ├─ 可以，出错成本低 → 自主式（Agent）
           ├─ 可以，出错成本高 → 辅助式（Copilot，AI 建议人确认）
           └─ 不可以 → AI 是增强还是核心？
                       ├─ 增强 → 嵌入式（AI 在幕后）
                       └─ 核心 → 对话式 + 辅助式混合
```

### 选什么 AI 技术方案

```
任务需要外部知识吗？
  ├─ 需要 → 加 RAG
  └─ 不需要 → 纯 Prompt 够吗？
                ├─ 够 → 纯 Prompt
                └─ 不够 → 需要多步推理吗？
                          ├─ 需要 → Agent（规划 + 工具调用）
                          └─ 不需要 → 微调模型
```

## 4.6 AI 产品设计的常见错误

| 错误 | 表现 | 正确做法 |
|------|------|---------|
| 为 AI 而 AI | 不需要 AI 的地方硬塞 AI | 先问"不用 AI 能不能做" |
| AI 打头，体验垫底 | 能力强但交互烂 | 把 AI 输出包装好——格式清晰、延迟可控、错误友好 |
| 过度承诺 | 写"100% 准确" | 说"AI 辅助分析，建议人工复核" |
| 没有评测就上线 | 感觉"差不多"就上线 | 上线前至少跑一轮离线评测 |
| 上线后不管了 | 上线庆祝，然后没人管 | 建立 Badcase 收集→分析→修复→验证闭环 |

---

# 第五篇：面试高频考点

## 5.1 必须能讲清楚的概念

- **RAG 的原理和适用场景**：检索→增强→生成三步，什么时候用 RAG 什么时候微调
- **Prompt Engineering 的产品化**：prompt 的生命周期管理——怎么写、怎么测、怎么迭代、怎么 A/B
- **评测体系设计**：不是"准确率"，是 MECE 维度、样本来源、评分标准、自动化执行、badcase 闭环
- **Agent 架构选型**：Fixed Workflow vs Sub-agent vs Agent Team，说清楚什么时候用哪个
- **幻觉控制**：不是"优化 prompt"，是 RAG 锚定、输出验证、人工兜底

## 5.2 必问题与参考答案

**"你做的 AI 产品和普通产品有什么区别？"**

> 核心区别在于不确定性管理。普通产品的输入输出是确定的——点按钮就跳转。AI 产品的输出不确定——同一个 prompt 两次结果可能不同。所以我的工作重心不是"做功能"，而是"建评测体系、管 Badcase 闭环、控制输出质量"。

**"你在这个项目里具体做了什么？"**

> 不要说"我负责产品设计"。要说：我定义了用户场景，拆解了产品链路，设计了评测体系，定位了 Badcase 并修复。每个动作都要有具体的技术名词和量化结果。

**"你怎么衡量 AI 产品做得好不好？"**

> 三个层次：基础层——格式正确率、响应时间、幻觉率；任务层——意图理解准确率、检索命中率、任务完成率；用户层——满意度、留存率、复访率。我在第二个层次，用 Golden Set 做离线评测。

**"你做的 Agent 和普通 Chatbot 有什么区别？"**

> 普通 Chatbot 是一问一答，无状态。我的 Agent 有意图识别、多步推理、工具调用能力。比如用户说"帮我分析竞品"，Agent 会拆解成搜索、读取、对比、输出四步，而不是直接编一个回答。

**"你怎么保证 Agent 输出的质量？"**

> 三条线：事前——RAG 提供事实锚点，Prompt 约束输出格式和边界；事中——LLM-as-a-Judge 实时评分；事后——Badcase 归因到具体链路节点，迭代修复后用同一评测集验证。

**"RAG 和微调有什么区别？什么时候用哪个？"**

> RAG 适合知识频繁更新、需要可解释性、有明确知识库的场景。微调适合需要模型学会某种风格、推理模式、或特定领域语言习惯的场景。RAG 改的是"输入"，微调改的是"模型本身"。成本和维护方式完全不同。

**"你的 Agent 如果出错了，你怎么排查？"**

> 沿链路逐节点排查：意图理解对不对？检索结果对不对？拼接的上下文对不对？Prompt 逻辑对不对？模型输出对不对？每个节点有明确的输入输出，出问题能定位到具体节点，而不是笼统说"模型答错了"。

**"LLM 的幻觉问题你怎么解决？"**

> 三层防护：第一层，RAG 提供事实锚点，让模型基于检索到的真实信息回答；第二层，输出验证，用 LLM-as-a-Judge 检查输出中是否有与检索结果矛盾的内容；第三层，高风险场景转人工。幻觉不可能完全消除，产品经理的职责是把它控制在可接受范围内。

**"你怎么控制 API 成本？"**

> 四个手段：缓存高频问题的结果；意图识别前置，简单问题走正则不调 LLM；分层模型，简单任务用小模型，复杂任务用大模型；控制上下文长度，不把无关信息塞进 prompt。

## 5.3 进阶题与参考答案

**"你对 AI 产品经理这个岗位的理解是什么？"**

> 传统 PM 的核心能力是需求分析、产品设计、项目管理。AI PM 多了三个：技术边界感（知道 AI 能做什么不能做什么）、评测体系设计（能定义什么叫"好"）、Badcase 驱动的迭代能力（不是上线就完了，是持续发现和修复问题）。本质上，AI PM 是"不确定性管理者"。

**"你觉得 AI 产品最难的是什么？"**

> 不是技术，是预期管理。用户以为 AI 什么都能做，但实际上 AI 会犯错、会幻觉、会不稳定。产品经理要做的不是让 AI 不犯错（做不到），而是让用户在 AI 犯错时能理解、能纠正、能继续用。

**"你怎么看 AI 产品经理会不会被 AI 取代？"**

> 执行层会被取代——写 PRD、画原型这些 AI 已经能做。但判断层不会被取代——定义什么场景该用 AI、选择什么技术方案、设计评测标准、判断 Badcase 优先级、和 stakeholder 沟通取舍。这些需要的是判断力，不是执行力。

**"你设计产品时怎么考虑 AI 的不确定性？"**

> 三个手段：一是限制 AI 的输出范围——约束在结构化输出里；二是给用户"退路"——AI 输出错了用户可以一键修改、重试、跳过；三是分级使用——低风险场景 AI 自动处理，高风险场景转人工确认。

**"你怎么说服团队用 AI 而不是传统方案？"**

> 先算账。AI 方案的成本（API 费用、延迟、准确率）和传统方案的成本（开发时间、维护成本、规则复杂度）对比。如果 AI 方案在成本可接受范围内，能解决传统方案解决不了的问题（如语义理解、开放式问答），就值得用。

**"你觉得 AI 产品的护城河是什么？"**

> 不是模型本身——模型是商品，大家都能用。护城河是：数据飞轮（用户越多数据越多模型越好）、评测体系（你能精确衡量好坏，竞品靠感觉）、垂直场景的理解（通用 AI 不知道你的用户要什么）。技术可以复制，但你对用户场景的理解和你的数据飞轮复制不了。

## 5.4 面试防御话术

**当面试官质疑你的项目"太简单"**

> 这个项目看起来简单，但每个节点都有产品判断。比如意图识别我选了正则+LLM 兜底，没用 BERT 微调，因为现阶段意图类型只有 5 种，正则够用。架构上我选了 Fixed Workflow，没上 Agent Team，因为面试评分场景链路固定。每个技术选型我都能说出为什么。

**当面试官质疑你的数据"样本太小"**

> 30 题确实不算大，但这是我的 Golden Set 而不是上线数据。Golden Set 的价值不是样本量大，是每道题都有人工标注的黄金标准，能精确衡量每次迭代的效果。30 题的五维评分对标 30 道题的人工标注，这是一套完整的评测闭环。

**当面试官质疑"你只是调了 Prompt"**

> 调 Prompt 只是动作，重点是调之前的诊断和调之后的验证。我定位到问题是第三层任务指令缺少岗位锚定逻辑，不是笼统地"改了一下 prompt"。改完用同一套 Golden Set 验证，岗位匹配度从 2.1 到 4.3。这个过程和传统 PM 的"发现需求→设计方案→上线验证"没有本质区别，只是我的"代码"是 Prompt。

---

# 第六篇：行业格局与深度主题

> 面试官越资深，越可能问到你"怎么看这个行业"。这一篇覆盖模型对比、算力成本、数据工程、推理优化、商业化、多模态、安全对齐和行业趋势——让你在面试中能有判断、有观点、有数据。

## 6.1 主流模型横向对比（2025-2026）

### 闭源模型对比

| 模型 | 公司 | 上下文窗口 | 多模态 | 核心优势 | 核心劣势 | 定价（输入/输出 每百万token） |
|------|------|----------|--------|---------|---------|------|
| GPT-4o | OpenAI | 128K | 文本+图像+音频 | 综合推理最强、生态最成熟 | 贵、延迟波动 | $2.50 / $10.00 |
| GPT-4o-mini | OpenAI | 128K | 文本+图像 | 性价比高、速度快 | 复杂推理弱于 GPT-4o | $0.15 / $0.60 |
| Claude 3.5 Sonnet | Anthropic | 200K | 文本+图像 | 长文本理解、代码能力、安全性 | 多模态不如 GPT-4o | $3.00 / $15.00 |
| Claude 3.5 Haiku | Anthropic | 200K | 文本+图像 | 速度最快、成本极低 | 推理弱于 Sonnet | $0.80 / $4.00 |
| Gemini 2.0 Flash | Google | 1M | 文本+图像+音频+视频 | 超长上下文、多模态最强 | 中文弱于国产模型 | $0.10 / $0.40 |
| Gemini 2.0 Pro | Google | 2M | 全模态 | 最强上下文窗口 | 贵、生态不如 OpenAI | $1.25 / $5.00 |

### 国产模型对比

| 模型 | 公司 | 上下文窗口 | 核心优势 | 适用场景 |
|------|------|----------|---------|---------|
| DeepSeek-V3 | 深度求索 | 128K | MoE 架构，性价比极高，推理能力接近 GPT-4o | 对成本敏感的大规模推理 |
| DeepSeek-R1 | 深度求索 | 128K | 推理增强，CoT 深度思考 | 数学、代码、逻辑推理 |
| Qwen2.5-Max | 阿里 | 128K | 中文能力最强之一，多尺寸覆盖 | 中文场景首选 |
| 文心一言 4.0 | 百度 | 128K | 中文理解、搜索增强 | 需要搜索增强的中文场景 |
| 混元 Turbo | 腾讯 | 256K | 长上下文、价格低 | 企业微信/腾讯生态 |
| 豆包 Pro | 字节 | 128K | 多模态、价格激进 | 扣子生态、C端应用 |
| GLM-4 | 智谱 | 128K | 中文优化、工具调用强 | Agent 开发 |
| MiniMax-01 | MiniMax | 4M | 超长上下文（全球最长） | 长文档处理 |
| Kimi | 月之暗面 | 128K | 长文本理解、搜索 | 信息检索与整理 |

### 开源模型对比

| 模型 | 参数 | 上下文 | 亮点 | 适用场景 |
|------|------|--------|------|---------|
| LLaMA 3.3 70B | 70B | 128K | Meta 最新，综合能力最强开源模型 | 私有化部署首选 |
| DeepSeek-V3 | 671B MoE | 128K | 开源，性能接近 GPT-4o | 性价比私有化 |
| Qwen2.5-72B | 72B | 128K | 中文最强开源 | 中文场景私有化 |
| Mistral Large | 123B | 128K | 欧洲最强，多语言 | 欧洲市场、多语言 |
| Yi-Lightning | 未公开 | 256K | 长上下文、低成本 | 长文档处理 |

### 模型选型决策矩阵

```
你的场景是什么？
├─ 通用对话/Agent → GPT-4o（最强）或 DeepSeek-V3（性价比）
├─ 长文档处理 → Gemini 2.0 Pro（2M）或 MiniMax-01（4M）
├─ 中文场景 → Qwen2.5-Max 或 DeepSeek-V3
├─ 代码生成 → Claude 3.5 Sonnet 或 DeepSeek-R1
├─ 多模态理解 → GPT-4o 或 Gemini 2.0 Flash
├─ 私有化部署 → LLaMA 3.3 70B 或 DeepSeek-V3
├─ 成本敏感 → DeepSeek-V3 或 GPT-4o-mini
└─ 超低延迟 → Claude 3.5 Haiku 或 Gemini 2.0 Flash
```

## 6.2 算力、训练与成本

### 算力基础概念

| 概念 | 是什么 | 为什么重要 |
|------|--------|----------|
| FLOPs | 浮点运算次数，衡量计算量 | 训练一个模型需要多少算力 |
| GPU | 图形处理器，AI 训练的主力芯片 | NVIDIA H100 是行业标配 |
| TPU | Google 自研 AI 芯片 | Google 内部训练用，不对外卖 |
| 显存（VRAM） | GPU 的内存 | 决定能跑多大的模型 |
| 算力集群 | 多台 GPU 服务器互联 | 千卡集群是训练大模型的门槛 |

### 训练大模型需要多少算力

| 模型规模 | 训练算力（估算） | GPU 数量 | 训练时间 | 估算成本 |
|---------|----------------|---------|---------|---------|
| 7B 参数 | ~1e22 FLOPs | 64 张 H100 | 约 1-2 周 | $50万-100万 |
| 70B 参数 | ~1e23 FLOPs | 256 张 H100 | 约 1-3 个月 | $200万-500万 |
| 405B 参数（LLaMA 3） | ~4e24 FLOPs | 16,000 张 H100 | 约 2-3 个月 | 估算 $5,000万+ |
| GPT-4 级别 | 未公开，估计 ~1e25 FLOPs | 25,000 张 A100 | 约 3-6 个月 | 估算 $1 亿+ |

**产品视角**：知道这些数字不是为了炫技，而是为了理解"为什么大模型 API 这么贵"和"为什么不是谁都能做基座模型"。训练 GPT-4 级别的模型，光算力就要上亿美元，这还是没算数据采集、人工标注、实验试错。

### 推理成本

| 模型 | 输入 $/1M token | 输出 $/1M token | 一次对话约多少 token | 一次对话约多少钱 |
|------|------|------|------|------|
| GPT-4o | $2.50 | $10.00 | 输入 500 + 输出 300 | ~$0.004 |
| GPT-4o-mini | $0.15 | $0.60 | 同上 | ~$0.0003 |
| DeepSeek-V3 | ¥1 | ¥2（约$0.14/$0.28） | 同上 | ~¥0.001 |
| Claude 3.5 Sonnet | $3.00 | $15.00 | 同上 | ~$0.006 |

**产品判断**：做 C 端产品，一次对话几分钱，日活 100 万每天要烧几万刀。所以做 C 端 AI 产品必须算清楚单位经济学——用户价值是否大于推理成本。

### 推理成本控制策略

| 策略 | 做法 | 节省幅度 |
|------|------|---------|
| 模型分级 | 简单任务用小模型，复杂任务用大模型 | 30-50% |
| 缓存 | 相同问题直接返回缓存结果 | 视重复率而定 |
| Prompt 压缩 | 去掉 Prompt 中的冗余，减少 token 消耗 | 10-30% |
| 量化推理 | 用 INT8/INT4 精度推理 | 50-70% |
| 批处理 | 合并非实时请求批量处理 | 视场景而定 |
| 选择 MoE 模型 | 推理时只激活部分参数 | 30-50% |

### GPU 市场格局

| GPU | 厂商 | 显存 | 定位 | 市场价（2025） |
|------|------|------|------|------|
| H100 | NVIDIA | 80GB | 训练主力 | $25,000-35,000/张 |
| H200 | NVIDIA | 141GB | H100 升级版 | $30,000-40,000/张 |
| B200 | NVIDIA | 192GB | Blackwell 架构，最强 | $40,000+ |
| A100 | NVIDIA | 40/80GB | 上一代主流 | $10,000-15,000/张 |
| 昇腾 910B | 华为 | 64GB | 国产替代首选 | 受制裁影响，价格不透明 |

**产品视角**：了解 GPU 格局是为了理解"算力卡脖子"——美国对华芯片出口管制直接影响国产大模型的训练能力。面试时提到这一点，说明你有关注行业宏观格局。

## 6.3 数据工程

### 训练数据的构成

大模型训练数据通常包含：

| 数据类型 | 占比（估算） | 来源 |
|---------|------------|------|
| 网页文本 | 60-70% | Common Crawl、维基百科 |
| 书籍 | 5-10% | Books3、图书馆数字化 |
| 代码 | 10-15% | GitHub、Stack Overflow |
| 学术论文 | 3-5% | arXiv、PubMed |
| 对话数据 | 3-5% | Reddit、论坛 |
| 多语言数据 | 5-10% | 各语种网页 |

### 数据清洗流程

```
原始数据 → ①去重（MinHash）→ ②质量过滤（去低质、去广告）→ ③去隐私（去 PII）→ ④去毒（去仇恨/暴力）→ ⑤格式统一 → 训练数据
```

**产品意义**：数据质量比数据量更重要。GPT-3 用了 45TB 原始文本，但经过清洗后实际训练数据远小于此。面试时如果被问到数据相关话题，能说出"数据清洗是训练流程中最被低估的环节"就很加分。

### 数据标注

| 标注类型 | 用途 | 难点 |
|---------|------|------|
| SFT 数据 | 教模型对话 | 需要高质量、多样化的问答对 |
| RLHF 偏好数据 | 教模型什么回答更好 | 标注员之间一致性低 |
| 评测数据 | 验证模型表现 | 需要领域专家标注 |
| 红队数据 | 测试安全边界 | 需要创造性的攻击思维 |

### 合成数据

用 AI 生成训练数据，再用这些数据训练 AI。这是当前数据工程的热门方向。

**适用场景**：真实数据难以获取（如医疗、法律）、需要大量特定格式数据、数据增强。

**风险**：模型自噬——用 AI 生成的数据训练 AI，几轮后质量退化。合成数据需要人工抽检验证。

## 6.4 推理优化与部署

### 模型量化

| 精度 | 说明 | 性能损失 | 内存节省 |
|------|------|---------|---------|
| FP32 | 全精度，32 位浮点 | 无 | 基准 |
| FP16 | 半精度，16 位浮点 | 极小 | ~50% |
| INT8 | 8 位整数 | 轻微 | ~75% |
| INT4 | 4 位整数 | 中等 | ~87% |

**产品意义**：量化让大模型能在消费级硬件上跑。LLaMA 7B 量化到 INT4 后，可以在 MacBook 上运行。

### 知识蒸馏

用大模型（Teacher）的输出训练小模型（Student）。小模型学习大模型的"判断"，而不只是"正确答案"。

**产品意义**：蒸馏是"把大模型的能力压缩到小模型"的核心手段。字节的豆包、阿里的 Qwen 小尺寸版本都用了蒸馏。

### 推理框架

| 框架 | 特点 | 适用场景 |
|------|------|---------|
| vLLM | PagedAttention 技术，吞吐量极高 | 高并发生产环境 |
| TensorRT-LLM | NVIDIA 官方，极致优化 | NVIDIA GPU 部署 |
| Ollama | 一键部署，简单易用 | 本地开发、个人使用 |
| llama.cpp | CPU 推理，纯 C++ | 无 GPU 环境 |
| SGLang | 结构化生成，RadixAttention | 需要结构化输出的场景 |

**产品意义**：选推理框架是技术决策，但产品经理需要知道"为什么我们的 API 延迟高"——可能是推理框架没选对。

### 推理部署架构

```
用户请求 → 负载均衡 → API 网关 → 推理服务集群（vLLM） → 模型 → 流式返回
                                ↓
                          缓存层（Redis）
                                ↓
                          监控（延迟/吞吐/成本）
```

## 6.5 AI 产品商业化

### 定价模型

| 模式 | 做法 | 代表产品 | 优点 | 缺点 |
|------|------|---------|------|------|
| 按量计费 | 按 token/调用次数收费 | OpenAI API | 公平、灵活 | 收入不可预测 |
| 订阅制 | 月费/年费 | ChatGPT Plus、Cursor | 收入稳定 | 重度用户成本高 |
| 混合制 | 免费额度 + 付费超额 | Claude、Gemini | 获客+变现平衡 | 额度设计难 |
| 按结果收费 | 按任务完成收费 | 部分 AI 招聘产品 | 用户感知价值强 | 定义"完成"难 |
| 企业定制 | 私有化部署年费 | 各类 toB AI 产品 | 客单价高 | 销售周期长 |

### 单位经济学速算

以 C 端 AI 对话产品为例：

```
假设：
- 日活用户 100 万
- 每人每天 10 次对话
- 每次对话平均 2000 token（输入+输出）
- 使用 GPT-4o-mini：$0.0003/次

每日推理成本 = 100万 × 10 × $0.0003 = $3,000
每月推理成本 ≈ $90,000

如果订阅价 $20/月，付费率 3%：
月收入 = 100万 × 3% × $20 = $600,000
毛利率 = ($600,000 - $90,000) / $600,000 ≈ 85%

如果使用 GPT-4o（贵 10 倍）：
毛利率降到 35%——模型选错了，商业模式直接崩。
```

### 当前 AI 产品的主流商业模式

| 类型 | 产品 | 收入模式 | 核心壁垒 |
|------|------|---------|---------|
| API 层 | OpenAI、Anthropic | 按 token 计费 | 模型能力 |
| 平台层 | Coze、Dify | 免费 + 增值 | 生态和工具链 |
| 应用层-Chat | ChatGPT、Kimi | 订阅制 | 用户粘性和品牌 |
| 应用层-Copilot | Cursor、GitHub Copilot | 订阅制 | 工作流嵌入深度 |
| 应用层-Agent | 各类垂直 Agent | 订阅/按结果 | 场景理解和数据飞轮 |
| 基础设施 | 向量数据库、观测平台 | 按量/订阅 | 技术壁垒 |

## 6.6 多模态技术深度

### 视觉语言模型（VLM）

让模型同时理解图像和文本。代表：GPT-4V、Claude 3 Vision、Gemini 2.0。

**技术路线**：图像编码器（如 ViT）+ 文本 LLM + 对齐层（把图像特征映射到文本空间）。

**产品场景**：图片问答、OCR、UI 理解、图表分析、医疗影像辅助。

### 图像生成：扩散模型

Stable Diffusion、DALL-E、Midjourney 的核心技术。

**原理（通俗）**：给一张清晰的图逐步加噪→变成纯噪声→训练模型反向去噪→从噪声生成图片。

**关键概念**：
- **Prompt 跟随**：模型多准确理解你的文字描述
- **CFG Scale**：控制 prompt 影响力的强度
- **ControlNet**：精确控制构图、姿态、深度

### 视频生成

| 模型 | 公司 | 特点 | 当前状态 |
|------|------|------|---------|
| Sora | OpenAI | 物理世界模拟、长视频 | 已开放，生成质量领先 |
| Veo 2 | Google | 高分辨率、长时长 | 已开放 |
| Kling（可灵）| 快手 | 中文生态、短视频 | 国内领先 |
| 即梦 | 字节 | 扣子生态 | 已开放 |
| Runway Gen-3 | Runway | 专业视频编辑 | 已开放，影视行业应用 |

### 语音技术

| 能力 | 技术 | 代表 |
|------|------|------|
| TTS（文字转语音）| VITS、Tortoise | ElevenLabs、字节语音 |
| STT（语音转文字）| Whisper、Conformer | OpenAI Whisper |
| 实时语音对话 | 端到端语音模型 | GPT-4o 语音模式 |

**产品趋势**：语音正在从"辅助"变成"主交互"。GPT-4o 的实时语音让人和 AI 的对话延迟降到 200ms 以内，接近人类对话体验。

## 6.7 AI 安全与对齐深度

### 对齐的层次

```
第一层：有用（Helpful）——模型能完成用户的任务
第二层：诚实（Honest）——模型不编造，承认不知道
第三层：无害（Harmless）——模型拒绝有害请求
```

这三层是 Anthropic 提出的"HHH 原则"，是当前对齐研究的核心框架。

### 幻觉的类型与应对

| 幻觉类型 | 表现 | 应对 |
|---------|------|------|
| 事实性幻觉 | 编造不存在的事实、人物、事件 | RAG 锚定、来源标注 |
| 忠实性幻觉 | 输出与输入矛盾 | 输出验证、一致性检查 |
| 逻辑性幻觉 | 推理过程错误 | CoT 验证、多步校验 |

### 安全攻击类型

| 攻击 | 怎么做 | 防护 |
|------|--------|------|
| Prompt 注入 | 在用户输入中嵌入指令覆盖系统 Prompt | 输入过滤、角色分离、指令优先级 |
| 越狱（Jailbreak） | 用角色扮演、多语言、编码等方式绕过限制 | 多层防护、输出审核 |
| 数据投毒 | 在训练数据中注入恶意样本 | 数据清洗、异常检测 |
| 成员推断攻击 | 判断某数据是否在训练集中 | 差分隐私 |
| 对抗样本 | 微调输入让模型输出错误结果 | 对抗训练 |

### 红队测试

组织专门团队模拟攻击，测试模型安全边界。OpenAI、Anthropic 在模型发布前都会做大规模红队测试。

**产品意义**：如果你的产品有用户输入→模型处理→输出给用户的链路，就必须考虑安全。面试时能说出"我们做了 Prompt 注入防护和输出审核"比"我们很安全"具体得多。

## 6.8 行业趋势与竞争格局

### 2025-2026 关键趋势

| 趋势 | 说明 | 产品影响 |
|------|------|---------|
| 推理时计算（Test-time Compute） | 推理时多花算力，让模型"再想想" | o1/R1 类推理模型出现，慢但更准 |
| Agent 化 | 从对话到自主执行 | Agent 框架成为标配 |
| 端侧 AI | 模型跑在手机/电脑上 | 苹果 Intelligence、高通 AI Engine |
| 多模态融合 | 文本+图像+音频+视频统一处理 | 产品交互从纯文本变为多模态 |
| 长上下文竞赛 | 从 128K 到 1M 到 4M | 文档处理、知识库场景爆发 |
| 推理成本骤降 | 一年降 10 倍以上 | AI 产品大规模商业化成为可能 |
| AI 原生应用 | 不是"加 AI 功能"，是"AI 是产品本身" | Cursor、Devin 改变开发范式 |
| 开源追赶闭源 | LLaMA 3、DeepSeek 接近 GPT-4 | 私有化部署不再妥协 |

### 竞争格局速览

| 阵营 | 玩家 | 策略 |
|------|------|------|
| 美国闭源 | OpenAI、Anthropic、Google | 用最强模型卡位高端市场 |
| 美国开源 | Meta（LLaMA） | 用开源生态对抗闭源 |
| 中国闭源 | 百度、阿里、字节、腾讯 | 中文场景+生态绑定 |
| 中国开源 | 深度求索、智谱、阿里 | 用开源+性价比出海 |
| 创业公司 | Mistral、MiniMax、月之暗面 | 单点突破（长上下文、多模态） |

### 对 AI PM 求职者的影响

1. **模型能力趋同**：GPT-4o、Claude 3.5、DeepSeek-V3 差距在缩小→产品差异化不靠模型，靠场景理解
2. **推理成本断崖式下降**：一年降 10 倍→AI 产品不需要"烧钱"了，商业模式跑得更快
3. **Agent 从概念到落地**：2025 是 Agent 元年→有 Agent 项目经验是加分项
4. **多模态不是未来，是现在**：纯文本产品正在被多模态产品替代→简历上要有涉及
5. **AI 安全监管趋严**：中国大模型备案、欧盟 AI Act→合规意识是加分项

### 面试中展示行业认知

**不要说**："AI 行业发展很快，我很看好。"

**要说**：

> "我关注到几个趋势：一是推理成本在急剧下降，DeepSeek 的定价把行业打下来了，这对 AI 产品规模化是利好；二是 Agent 从 Demo 走向生产，Coze、Dify 这些平台让 Agent 开发门槛大幅降低；三是模型能力趋同，产品差异化越来越依赖场景理解和数据飞轮，而不是模型本身。"
>
> "具体到 AIGC 面试这个场景，成本下降意味着我可以给用户更多免费额度，Agent 化意味着我可以从'评分'扩展到'模拟面试官追问'，模型趋同意味着我的护城河是面试评测体系和 Badcase 闭环。"

---

# 附录：术语速查表

| 术语 | 一句话解释 |
|------|----------|
| LLM | 大语言模型，GPT、Claude、文心一言都是 |
| Token | 文本最小单位，计费和上下文窗口都按它算 |
| Prompt | 给模型的输入文本 |
| System Prompt | 系统提示词，定义模型的行为边界 |
| RAG | 检索增强生成，先查知识库再回答 |
| Fine-tuning | 微调，用特定数据训练模型 |
| RLHF | 人类反馈强化学习，让模型更对齐人类偏好 |
| Hallucination | 幻觉，模型编造不存在的事实 |
| Embedding | 把文本转成向量 |
| Vector DB | 向量数据库，存 Embedding 的 |
| Agent | 能自主规划、调用工具、多步执行的 AI 系统 |
| Function Calling | 工具调用，模型决定调什么工具、传什么参数 |
| Temperature | 控制输出的随机性，越低越确定 |
| Context Window | 上下文窗口，模型一次能看到的 token 上限 |
| Latency | 延迟，从请求到返回的时间 |
| Throughput | 吞吐量，每秒处理多少请求 |
| Guardrails | 护栏，防止模型输出不安全内容 |
| Eval | 评测，衡量模型表现 |
| Golden Set | 黄金测试集，人工标注的标准答案集 |
| Badcase | 坏例，模型表现差的 case |
| Grounding | 事实锚定，让模型基于真实信息回答而非编造 |
| MECE | 互不重叠、共同穷尽，评测维度设计原则 |
| Fixed Workflow | 固定工作流，预定义不可变的执行步骤链 |
| Sub-agent | 子 Agent，主 Agent 把子任务派发给专门的小 Agent |
| Agent Team | Agent 团队，多个 Agent 各司其职协作完成任务 |
| Transformer | 基于自注意力机制的神经网络架构，GPT/BERT 的基础 |
| Self-Attention | 自注意力，让每个词和所有其他词计算关联度 |
| Encoder | 编码器，理解输入文本（BERT 只用它） |
| Decoder | 解码器，逐词生成输出（GPT 只用它） |
| MLM | 掩码语言模型，BERT 的训练方式——挖空猜词 |
| NSP | 下一句预测，BERT 判断两句是否连续 |
| MoE | 混合专家架构，每次推理只激活部分参数，降本增效 |
| Scaling Law | 规模定律，模型/数据/算力越大性能越好 |
| LLaMA | Meta 开源大模型系列，私有化部署首选 |
| NER | 命名实体识别，从文本中提取人名/地名/机构名 |
| FLOPs | 浮点运算次数，衡量模型训练/推理的算力消耗 |
| GPU | 图形处理器，AI 训练和推理的核心硬件 |
| H100 | NVIDIA 当前主流的 AI 训练 GPU，约 $3 万/张 |
| 量化 | 降低模型参数精度（FP32→INT8/INT4），减少显存和推理成本 |
| 蒸馏 | 用大模型（Teacher）训练小模型（Student），压缩能力 |
| vLLM | 高吞吐量推理框架，PagedAttention 技术 |
| VLM | 视觉语言模型，同时理解图像和文本 |
| 扩散模型 | 图像生成核心技术，从噪声反向去噪生成图片 |
| TTS | 文字转语音 |
| STT | 语音转文字 |
| HHH | Anthropic 对齐三原则：Helpful、Honest、Harmless |
| 红队测试 | 模拟攻击测试模型安全边界 |
| 合成数据 | AI 生成的数据用于训练 AI |
| 单位经济学 | 每用户收入 vs 推理成本，AI 产品商业化的核心指标 |
| Test-time Compute | 推理时计算，多花算力让模型"再想想"，o1/R1 的核心思想 |

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
