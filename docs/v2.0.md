# MindTraining v2.0 — AI 产品面试题库 + 智能解析

在 v1（产运思维训练）基础上升级为**产品岗垂直**面试训练工具。

## v2.0 已实现

| 模块 | 能力 |
|------|------|
| 题库 | `interview-bank-v2.json` **55 道**产品岗真题，公司/题型/难度三级分类 |
| 解析 | `/api/analyze`：回答框架 · 评分标准 · 参考对比 |
| 点评 | 产品岗五维评分（逻辑性/专业性/流畅度/匹配度/内容密度） |
| 语音 | `/api/transcribe`（Whisper，需 `OPENAI_API_KEY`） |
| 计时 | 前端模拟面试计时器 |
| 复盘 | `/api/analytics` 进步曲线 + 薄弱题型 |
| 评测集 | `eval/scoring-golden.json` 15 份 + `scripts/eval-scoring.js` Spearman |

## Coach 面经导入

```bash
node scripts/import-coach-docx.js
```

从 `Desktop/coach/面经_去重版.docx` 导入 **218 道** AI PM 题 + **精选 10 题话术** → `interview-bank-ai-pm.json` / `interview-bank-ai-pm-curated.json` / 更新 `interview-rag-feed.json`。

## 构建题库

```bash
node scripts/build-interview-bank-v2.js
```

## 跑评分一致性评测

```bash
DEEPSEEK_API_KEY=sk-xxx node scripts/eval-scoring.js
```

## 环境变量

| 变量 | 说明 |
|------|------|
| `DEEPSEEK_API_KEY` | 必填 |
| `OPENAI_API_KEY` | 语音转写（可选） |
| `PM_REVIEW_MODE` | 默认 1，产品岗五维点评 |

## 待你手动完成（规划第 1–6 天）

1. 从牛客/小红书/知乎补充带原链接的真题到 Excel，再导入 feed 或 bank
2. 找 1–2 同学对 `eval/scoring-golden.json` 做人工复核标注
3. 跑 eval-scoring 记录 Spearman，迭代 badcase prompt

## 面试防御话术

见项目规划文档「四、面试防御」— 垂直产品岗、15 份验证阶段、分维度打分防风格偏好。
